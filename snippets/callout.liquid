<div class="bg-blue-200 mt-8 mb-4 rounded-md p-4 text-blue-700">
    <div class="flex items-center gap-2">
        {% render 'icon', icon: 'moon' class: 'stroke-blue-700' %}
        <h4 class="text-inherit">{{ heading }}</h4>
    </div>
    <p class="body-small mt-3 border-t border-blue-700 pt-3">
        {{ content }}
    </p>
</div>